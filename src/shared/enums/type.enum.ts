export enum ETweetType {
  Tweet,
  Retweet,
  Comment,
  QuoteTweet
}

export enum EMediaType {
  Image,
  Video
}

export enum TokenType {
  accessToken,
  refreshToken,
  forgotPasswordToken,
  verifyToken
}
